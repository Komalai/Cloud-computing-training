# Day 9 Training Diary – Docker Volumes & Dockerizing a React Project

**Date:** 02/07/2026

## Topics Covered

* Docker Volumes
* Managing Docker Volumes
* Dockerizing a React Application
* Creating a Dockerfile
* Using a `.dockerignore` File
* Building and Running a React Docker Image

---

# Docker Volumes

Docker Volumes are used to store data outside a container so that it remains available even after the container is removed.

### Advantages

* Persistent data storage
* Share data between containers
* Easy backup and restore
* Better performance than bind mounts

---

# Why Use Docker Volumes?

Without a volume, container data is lost when the container is deleted. Volumes keep the data safe and allow it to be reused by new containers.

```text
Container
     │
     ▼
Docker Volume
     │
Persistent Data
```

---

# Docker Volume Commands

Create a volume:

```bash
docker volume create myvolume
```

List all volumes:

```bash
docker volume ls
```

Inspect a volume:

```bash
docker volume inspect myvolume
```

Remove a volume:

```bash
docker volume rm myvolume
```

Run a container with a volume:

```bash
docker run -d -v myvolume:/app/data nginx
```

---

# Dockerizing a React Project

Dockerizing means packaging a React application into a Docker image so it can run consistently on different systems.

### Steps Performed

1. Create a React application.

```bash
npx create-react-app my-react-app
```

2. Open the project in VS Code.
3. Create a `Dockerfile`.
4. Build the Docker image.
5. Run the container.

---

# Sample Dockerfile

```dockerfile
FROM node:18

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE 3000
Here's Day 4 in the same concise, notes-style format. I kept the theory short, changed the wording, and made it look like personal study notes instead of copied content.

Day 4 Training Diary – AWS VPC (Virtual Private Cloud)
Date: 25/06/2026

Topics Covered
Introduction to Amazon VPC

VPC Components

Public and Private Subnets

Internet Gateway (IGW)

Route Tables

Network ACL (NACL)

NAT Gateway

Secure Communication between EC2 and RDS

Creating a VPC

Launching an EC2 Instance in a VPC

Amazon VPC
Amazon VPC (Virtual Private Cloud) is a networking service that allows users to create a private network in AWS. It helps organize cloud resources securely and gives control over IP addresses, subnets, and routing.

Why VPC?
Provides network isolation

Improves security

Controls internet access

Allows custom network configuration

VPC Components
Subnet
A subnet is a smaller network inside a VPC.

Public Subnet

Connected to the Internet Gateway

Used for web servers

Accessible from the internet

Private Subnet

No direct internet access

Used for databases and backend services

More secure than public subnets

Internet Gateway (IGW)
An Internet Gateway connects a VPC to the internet and allows communication between AWS resources and external users.

Route Table
A Route Table defines how network traffic is routed within the VPC.

Destination	Target
Local	Local VPC
0.0.0.0/0	Internet Gateway
Network ACL (NACL)
A Network ACL is a subnet-level firewall that controls incoming and outgoing traffic.

Features

Works at subnet level

Supports Allow and Deny rules

Stateless

NAT Gateway
A NAT Gateway allows instances in a private subnet to access the internet without making them publicly accessible.

Uses

Software updates

Package installation

Secure outbound internet access

EC2 and RDS Architecture
For better security, the database should remain inside a private subnet instead of being exposed to the internet.

Internet
    │
Internet Gateway
    │
Public Subnet
    │
EC2 Instance
    │
Private Subnet
    │
RDS Database
Advantages

Better security

Database remains private

Reduced attack surface

Secure communication between EC2 and RDS

Creating a VPC
Steps performed:

Open AWS Management Console.

Search for VPC.

Click Create VPC.

Enter VPC name and CIDR block.

Create Public and Private Subnets.

Attach an Internet Gateway.

Create Route Tables.

Configure Network ACLs and Security Groups.

Example

VPC Name: MyVPC
CIDR Block: 10.0.0.0/16

Public Subnet : 10.0.1.0/24
Private Subnet: 10.0.2.0/24
Launching an EC2 Instance
Steps:

Open the EC2 Dashboard.

Click Launch Instance.

Select an AMI and Instance Type.

Choose the created VPC.

Select the required subnet.

Configure the Key Pair and Security Group.

Launch the instance.

Best Practices
Deploy web servers in Public Subnets.

Keep databases in Private Subnets.

Use Security Groups for instance security.

Use NACLs for subnet protection.

Use a NAT Gateway for secure internet access from private instances.

Summary
Today's session introduced AWS VPC and its networking components. I learned how to create a custom VPC, configure subnets, route tables, Internet Gateway, and NAT Gateway, and understand how EC2 and RDS communicate securely within a VPC.


# Training Diary – Weekly Revision

**Date:** 26/06/2026

## Today I Learned

Today was dedicated to revising all the AWS concepts covered during the first week of training. We reviewed the fundamental cloud computing concepts, Linux commands, IAM, EC2, S3, and VPC to strengthen our understanding and clear any doubts.

---

# Weekly Revision Topics

## 1. Cloud Computing

### Important Points

- Cloud computing is the delivery of IT resources over the internet.
- Resources are available on demand.
- Pay only for the resources used.
- Highly scalable and reliable.

### Cloud Service Models

| Model | Description | Example |
|--------|-------------|---------|
| IaaS | Infrastructure as a Service | Amazon EC2 |
| PaaS | Platform as a Service | AWS Elastic Beanstalk |
| SaaS | Software as a Service | Gmail, Spotify |

---

# 2. AWS Basics

### AWS Benefits

- Global Infrastructure
- High Availability
- Elastic Scalability
- Secure Services
- Pay-as-you-go Pricing

### Important AWS Services

- EC2
- S3
- IAM
- VPC
- RDS
- Lambda
- CloudWatch

---

# 3. Linux Commands Revision

| Command | Purpose |
|----------|----------|
| pwd | Show current directory |
| whoami | Display logged-in user |
| ls | List files |
| ls -l | Detailed file list |
| ls -lt | Sort files by latest modification |
| ls -a | Show hidden files |
| cd | Change directory |
| mkdir | Create directory |
| rmdir | Remove empty directory |
| touch | Create file |
| cat | Display file contents |
| cp | Copy files |
| mv | Move or rename files |
| rm | Delete files |
| date | Display system date |
| clear | Clear terminal |

---

# 4. IAM (Identity and Access Management)

### Key Concepts

- Create Users
- Create Groups
- Assign Permissions
- Roles
- Policies
- Multi-Factor Authentication (MFA)

### Why IAM?

- Secure AWS resources
- Control user permissions
- Prevent unauthorized access

---

# 5. Amazon EC2

### Important Concepts

- Virtual Server
- Amazon Machine Image (AMI)
- Instance Types
- Key Pair
- Security Group
- Public IP Address

### EC2 Instance Creation Steps

1. Launch Instance
2. Select AMI
3. Choose Instance Type
4. Create Key Pair
5. Configure Network
6. Configure Security Group
7. Launch Instance

### Security Group Ports

| Port | Purpose |
|------|---------|
| 22 | SSH |
| 80 | HTTP |
| 443 | HTTPS |

---

# 6. Amazon S3

### Important Concepts

- Bucket
- Object
- Bucket Policy
- Static Website Hosting
- Versioning
- Lifecycle Rules

### Storage Classes

- S3 Standard
- Intelligent-Tiering
- Standard-IA
- One Zone-IA
- Glacier Instant Retrieval
- Glacier Flexible Retrieval
- Glacier Deep Archive

### Uses of S3

- File Storage
- Website Hosting
- Backup
- Archive
- Media Storage

---

# 7. Amazon VPC

### Components

- VPC
- Public Subnet
- Private Subnet
- Internet Gateway
- Route Table
- Network ACL
- NAT Gateway
- Security Group

### Best Practice Architecture

text
Internet
    │
Internet Gateway
    │
Public Subnet
    │
EC2 Instance
    │
Private Subnet
    │
RDS Database


### Why Keep RDS Private?

- Better security
- Prevent direct internet access
- Reduced attack surface
- Secure communication through EC2

---

# Important Interview Questions Revised

### Q1. What is Cloud Computing?

Delivery of computing resources over the internet on a pay-as-you-go basis.

### Q2. Difference between IaaS, PaaS, and SaaS?

- IaaS provides infrastructure.
- PaaS provides a development platform.
- SaaS provides ready-to-use software.

### Q3. What is IAM?

IAM is used to manage users, groups, roles, and permissions securely in AWS.

### Q4. What is an EC2 Instance?

A virtual server running in AWS.

### Q5. What is an S3 Bucket?

A logical container used to store files (objects) in Amazon S3.

### Q6. What is VPC?

A private virtual network that securely hosts AWS resources.

### Q7. Difference between Security Group and Network ACL?

| Security Group | Network ACL |
|---------------|-------------|
| Works at Instance level | Works at Subnet level |
| Stateful | Stateless |
| Supports Allow rules only | Supports Allow and Deny rules |

---

# Key Takeaways

- Understood the fundamentals of cloud computing.
- Revised AWS core services: IAM, EC2, S3, and VPC.
- Practiced important Linux commands.
- Reviewed secure AWS architecture using VPC.
- Strengthened understanding of EC2 deployment and S3 storage.
- Cleared doubts from previous training sessions.

---

# Summary

Today's session focused on revising all the topics covered during the first week of AWS training. The revision reinforced my understanding of cloud computing concepts, Linux commands, IAM, EC2, S3, and VPC networking. Reviewing these core services has helped me build a stronger foundation for upcoming AWS topics and practical implementations.

Since this was a revision day, your diary should be much shorter than the other days. There's no need to rewrite all the theory again. Just mention what was revised and the important points.

Day 5 Training Diary – Weekly Revision
Date: 26/06/2026

Topics Revised
Cloud Computing Fundamentals

AWS Core Services

Linux Commands

IAM (Identity and Access Management)

Amazon EC2

Amazon S3

Amazon VPC

Cloud Computing
Revised the basic concepts of cloud computing, including its advantages and the three cloud service models:

IaaS

PaaS

SaaS

AWS Services
Reviewed the purpose of the main AWS services covered during the week.

Services Revised

EC2

S3

IAM

VPC

RDS

Lambda

CloudWatch

Linux Commands
Practiced commonly used Linux commands.

Command	Purpose
pwd	Current directory
whoami	Current user
ls	List files
cd	Change directory
mkdir	Create directory
touch	Create file
cp	Copy files
mv	Move/Rename files
rm	Delete files
cat	Display file content
clear	Clear terminal
IAM Revision
Reviewed:

Users and Groups

Roles

Policies

Permissions

Multi-Factor Authentication (MFA)

EC2 Revision
Revised the EC2 deployment process and its components.

Key Concepts

Instance

AMI

Instance Type

Key Pair

Security Group

Public IP

Ports

Port	Service
22	SSH
80	HTTP
443	HTTPS
S3 Revision
Topics covered:

Buckets and Objects

Static Website Hosting

Storage Classes

Lifecycle Rules

Versioning

VPC Revision
Reviewed the networking concepts:

Public and Private Subnets

Internet Gateway

Route Tables

Network ACL

NAT Gateway

Security Groups

Architecture Reviewed

Internet
    │
Internet Gateway
    │
Public Subnet
    │
EC2
    │
Private Subnet
    │
RDS
Interview Questions Practiced
What is Cloud Computing?

Difference between IaaS, PaaS, and SaaS

What is IAM?

What is an EC2 Instance?

What is an S3 Bucket?

What is Amazon VPC?

Security Group vs Network ACL

Summary
Today's session was a revision of all the topics covered during the first week of AWS training. It helped reinforce cloud computing fundamentals, AWS services, Linux commands, and networking concepts while also preparing for upcoming practical sessions and interview questions.

💡 For revision days, keep the diary 1–2 pages maximum. There's no need to explain every concept again since you've already documented them on previous days. Simply mention what was revised, any important commands, and a brief summary. This makes your diary cleaner and avoids unnecessary repetition.


also answer the questions

Since your trainer specifically revised the interview questions, it's a good idea to include short answers (1–2 lines each) instead of just listing the questions. That keeps your diary complete without becoming too lengthy.

Day 5 Training Diary – Weekly Revision
Date: 26/06/2026

Topics Revised
Cloud Computing Fundamentals

AWS Core Services

Linux Commands

IAM (Identity and Access Management)

Amazon EC2

Amazon S3

Amazon VPC

Cloud Computing
Revised the basics of cloud computing and its service models:

IaaS

PaaS

SaaS

AWS Services
Reviewed the core AWS services covered during the week.

Services Revised

EC2

S3

IAM

VPC

RDS

Lambda

CloudWatch

Linux Commands
Practiced commonly used Linux commands.

Command	Purpose
pwd	Show current directory
whoami	Show logged-in user
ls	List files and folders
cd	Change directory
mkdir	Create a directory
touch	Create a file
cat	View file contents
cp	Copy files
mv	Move or rename files
rm	Delete files
clear	Clear terminal screen
IAM Revision
Revised the following topics:

Users and Groups

Roles

Policies

Permissions

Multi-Factor Authentication (MFA)

EC2 Revision
Reviewed:

Instance

AMI

Instance Type

Key Pair

Security Group

Public IP Address

Common Ports

Port	Purpose
22	SSH
80	HTTP
443	HTTPS
S3 Revision
Topics covered:

Buckets and Objects

Static Website Hosting

Storage Classes

Lifecycle Rules

Versioning

VPC Revision
Revised networking components:

Public Subnet

Private Subnet

Internet Gateway

Route Table

Network ACL

NAT Gateway

Security Group

Architecture Reviewed

Internet
    │
Internet Gateway
    │
Public Subnet
    │
EC2
    │
Private Subnet
    │
RDS
Interview Questions
1. What is Cloud Computing?
Cloud computing is the delivery of computing resources such as servers, storage, and databases over the internet on a pay-as-you-go basis.

2. What is the difference between IaaS, PaaS, and SaaS?
IaaS: Provides virtual infrastructure like servers and storage.

PaaS: Provides a platform to build and deploy applications.

SaaS: Provides ready-to-use software over the internet.

3. What is IAM?
IAM (Identity and Access Management) is an AWS service used to manage users, groups, roles, and permissions securely.

4. What is an EC2 Instance?
An EC2 instance is a virtual server in AWS used to run applications.

5. What is an S3 Bucket?
An S3 bucket is a container used to store files (objects) in Amazon S3.

6. What is Amazon VPC?
Amazon VPC is a private virtual network that securely hosts AWS resources and controls network communication.

7. Difference between Security Group and Network ACL?
Security Group	Network ACL
Works at instance level	Works at subnet level
Stateful	Stateless
Supports only Allow rules	Supports both Allow and Deny rules
Summary
Today's session was dedicated to revising the AWS concepts covered during the first week of training. The revision strengthened my understanding of Cloud Computing, Linux commands, IAM, EC2, S3, and VPC while helping me prepare for practical tasks and interview questions.

This is the maximum level of detail I'd recommend for a revision day. It stays concise, avoids repeating full theory from previous days, and includes short interview answers for quick revision.


Today 11:10 PM
Training Diary – Introduction to Docker
Date: 29/06/2026

Today I Learned
Introduction to Docker and Containers
Difference between Virtual Machines and Containers
Steps to install Docker
Docker Images
Basic Docker Commands
Docker Containers
Docker Hub
Introduction to Docker
Docker is an open-source containerization platform that enables developers to build, package, and run applications in lightweight, portable containers.

Unlike virtual machines, Docker containers share the host operating system's kernel, making them faster and more efficient.

Advantages of Docker
Lightweight and fast
Platform independent
Easy application deployment
Efficient resource utilization
Faster startup time
Simplified application management
What is a Container?
A container is a lightweight, standalone package that contains everything needed to run an application, including:

Application code
Runtime
Libraries
Dependencies
Configuration files
Containers ensure that applications run consistently across different environments.

Virtual Machine vs Docker Container
Virtual Machine	Docker Container
Runs on a Hypervisor	Runs on Docker Engine
Includes a complete Guest OS	Shares Host OS Kernel
Heavyweight	Lightweight
Slower startup	Starts in seconds
Uses more RAM and Storage	Uses fewer resources
Better isolation	Faster deployment
Steps to Install Docker
On Ubuntu/Linux
Step 1
Update packages.

sudo apt update
Step 2
Install Docker.

sudo apt install docker.io -y
Step 3
Start Docker service.

sudo systemctl start docker
Step 4
Enable Docker at boot.

sudo systemctl enable docker
Step 5
Check Docker version.

docker --version
Step 6
Verify installation.

docker run hello-world
Docker Images
A Docker Image is a read-only template used to create containers.

It contains:

Application
Dependencies
Libraries
Environment settings
Popular Images:

Ubuntu
Nginx
MySQL
Redis
Python
Node.js
Docker Hub
Docker Hub is the official online repository where Docker images are stored.

It allows users to:

Download images
Upload custom images
Share applications
Search for official images
Website:

https://hub.docker.com

Basic Docker Commands
Check Docker Version
docker --version
Display Docker Information
docker info
Download an Image
docker pull ubuntu
List Downloaded Images
docker images
Search Images
docker search nginx
Docker Container Commands
List Running Containers
docker ps
List All Containers
docker ps -a
Run a Container
docker run ubuntu
Run Container in Background
docker run -d nginx
Run a Named Container
docker run --name mycontainer ubuntu
Start a Container
docker start mycontainer
Stop a Container
docker stop mycontainer
Restart a Container
docker restart mycontainer
Inspect a Container
docker inspect mycontainer
Displays detailed information about the container in JSON format.

View Container Logs
docker logs mycontainer
Execute Commands Inside a Running Container
docker exec -it mycontainer bash
Delete a Stopped Container
docker rm mycontainer
Delete an Image
docker rmi ubuntu
Docker Workflow
Dockerfile
      │
      ▼
Docker Image
      │
      ▼
Docker Container
      │
      ▼
Running Application
Key Points
Docker uses containers instead of virtual machines.
Images are templates used to create containers.
Containers are lightweight and portable.
Docker Hub stores official and custom Docker images.
Docker simplifies application deployment across different environments.
Summary
Docker is a powerful containerization platform that allows applications to run consistently across different systems. By using Docker images and containers, applications become portable, lightweight, and easy to deploy. Learning Docker is an essential step toward modern cloud computing and DevOps practices.

Here's your Day 6 diary rewritten in the same short, note-style format we've been following. It looks more like personal study notes than copied material.

Day 6 Training Diary – Introduction to Docker
Date: 29/06/2026

Topics Covered
Introduction to Docker

Containers

Virtual Machines vs Containers

Docker Installation

Docker Images

Docker Hub

Basic Docker Commands

Docker Container Commands

Introduction to Docker
Docker is an open-source platform used to develop, package, and run applications inside containers. It helps applications run consistently across different systems.

Advantages
Lightweight

Fast deployment

Platform independent

Efficient resource usage

Easy application management

What is a Container?
A container is a lightweight environment that includes everything an application needs to run.

It contains:

Application code

Runtime

Libraries

Dependencies

Configuration files

Virtual Machine vs Docker Container
Virtual Machine	Docker Container
Runs on Hypervisor	Runs on Docker Engine
Includes Guest OS	Shares Host OS Kernel
Heavyweight	Lightweight
Slower startup	Faster startup
Uses more resources	Uses fewer resources
Installing Docker (Ubuntu)
Steps

Update packages.

sudo apt update
Install Docker.

sudo apt install docker.io -y
Start Docker service.

sudo systemctl start docker
Enable Docker at boot.

sudo systemctl enable docker
Check Docker version.

docker --version
Verify installation.

docker run hello-world
Docker Images
A Docker Image is a template used to create containers.

Popular Images

Ubuntu

Nginx

MySQL

Redis

Python

Node.js

Docker Hub
Docker Hub is the official repository for Docker images.

Uses

Download images

Upload custom images

Search official images

Share applications

Website:

https://hub.docker.com
Basic Docker Commands
Command	Purpose
docker --version	Check Docker version
docker info	Display Docker information
docker pull ubuntu	Download an image
docker images	List downloaded images
docker search nginx	Search images
Docker Container Commands
Command	Purpose
docker ps	List running containers
docker ps -a	List all containers
docker run ubuntu	Run a container
docker run -d nginx	Run container in background
docker run --name mycontainer ubuntu	Create a named container
docker start mycontainer	Start a container
docker stop mycontainer	Stop a container
docker restart mycontainer	Restart a container
docker inspect mycontainer	View container details
docker logs mycontainer	View container logs
docker exec -it mycontainer bash	Access container terminal
docker rm mycontainer	Remove a container
docker rmi ubuntu	Remove an image
Docker Workflow
Dockerfile
     │
     ▼
Docker Image
     │
     ▼
Docker Container
     │
     ▼
Running Application
Key Points
Docker uses containers instead of virtual machines.

Images are used to create containers.

Containers are lightweight and portable.

Docker Hub stores Docker images.

Docker simplifies application deployment.

Summary
Today's session introduced Docker and its basic concepts. I learned how containers differ from virtual machines, how to install Docker, work with Docker images and containers, and use common Docker commands. This session provided a good foundation for learning containerization and DevOps.

This matches the style we've been using:

✅ Short theory (2–3 lines max)

✅ Commands preserved

✅ Tables where useful

✅ Original wording

✅ Looks like concise personal notes rather than copied class material


# Training Diary – Docker Hub, Docker Images & Port Binding

**Date:** 30/06/2026

## Today I Learned

- Docker Hub and its features
- Docker Images
- Pulling images from Docker Hub
- Port Binding in Docker
- Running containers using Docker Hub images
- Accessing Docker applications through the browser

---

# Docker Hub

Docker Hub is the official cloud-based repository for Docker images. It allows developers to store, share, and download container images.

Docker Hub contains:

- Official Images
- Community Images
- Private Repositories
- Public Repositories

Website:

text
https://hub.docker.com


## Advantages of Docker Hub

- Easy image sharing
- Access to official images
- Version management using tags
- Secure image storage
- Supports CI/CD integration

---

# Docker Images

A Docker Image is a read-only template used to create Docker containers.

An image contains:

- Application code
- Runtime
- Libraries
- Dependencies
- Environment variables

Popular Images available on Docker Hub:

- Ubuntu
- Nginx
- Apache
- MySQL
- Redis
- Python
- Node.js
- MongoDB

---

# Searching for Images

Search for an image:

bash
docker search nginx


Download an image:

bash
docker pull nginx


View downloaded images:

bash
docker images


---

# Steps to Run an Image from Docker Hub

### Step 1

Open Docker Hub and search for the required image.

Example:

text
Nginx


### Step 2

Copy the Docker pull command.

Example:

bash
docker pull nginx


### Step 3

Verify that the image has been downloaded.

bash
docker images


### Step 4

Run the image as a container.

bash
docker run nginx


### Step 5

View the running container.

bash
docker ps


---

# Port Binding

Port Binding maps a port inside the Docker container to a port on the host machine.

Syntax:

bash
docker run -p <host-port>:<container-port> <image-name>


Example:

bash
docker run -p 8080:80 nginx


Here:

- **8080** → Host machine port
- **80** → Container port

This allows users to access the application running inside the container through the host machine.

---

# Running an Nginx Container with Port Binding

Run the container:

bash
docker run -d -p 8080:80 --name my-nginx nginx


Explanation:

- -d → Run in detached (background) mode.
- -p 8080:80 → Bind host port 8080 to container port 80.
- --name my-nginx → Assign a custom name.
- nginx → Docker image.

---

# Accessing the Application

After the container starts, open your web browser and visit:

text
http://localhost:8080


If running on an AWS EC2 instance, use the public IP:

text
http://<EC2-Public-IP>:8080


The default Nginx welcome page should appear.

> **Note:** If using AWS EC2, ensure that port **8080** is allowed in the EC2 Security Group.

---

# Useful Docker Commands

## List Running Containers

bash
docker ps


## List All Containers

bash
docker ps -a


## Stop a Container

bash
docker stop my-nginx


## Start a Container

bash
docker start my-nginx


## Restart a Container

bash
docker restart my-nginx


## Remove a Container

bash
docker rm my-nginx


## Remove an Image

bash
docker rmi nginx


---



# Summary

Docker Hub provides a centralized platform for storing and sharing Docker images. By pulling images from Docker Hub and using port binding, applications can be quickly deployed and accessed from a local machine or a cloud server. Understanding Docker images and port mapping is essential for building and deploying containerized applications.

Here's your Day 7 diary in the same concise, notes-style format. I've kept the explanations short, changed the wording, and focused on what you learned in class.

Day 7 Training Diary – Docker Hub, Docker Images & Port Binding
Date: 30/06/2026

Topics Covered
Docker Hub

Docker Images

Pulling Images from Docker Hub

Port Binding

Running Containers

Accessing Applications through Browser

Docker Hub
Docker Hub is the official online repository for Docker images. It allows users to download, upload, and manage container images.

Features
Official and community images

Public and private repositories

Image versioning

Easy image sharing

Website

https://hub.docker.com
Docker Images
A Docker Image is a template used to create containers. It includes everything required to run an application.

Popular Images

Ubuntu

Nginx

Apache

MySQL

Redis

Python

Node.js

MongoDB

Working with Images
Search for an image:

docker search nginx
Download an image:

docker pull nginx
View downloaded images:

docker images
Running a Container
Steps performed:

Search the required image on Docker Hub.

Pull the image.

Verify the downloaded image.

Run the container.

Check running containers.

Commands

docker pull nginx
docker images
docker run nginx
docker ps
Port Binding
Port Binding connects a container port with a host machine port so that the application can be accessed from a browser.

Syntax

docker run -p <host-port>:<container-port> <image-name>
Example

docker run -p 8080:80 nginx
Explanation

8080 → Host machine port

80 → Container port

Running Nginx with Port Binding
docker run -d -p 8080:80 --name my-nginx nginx
Options Used

-d → Run in background

-p → Port mapping

--name → Assign container name

nginx → Docker image

Accessing the Application
Open the browser and enter:

http://localhost:8080
For an AWS EC2 instance:

http://<EC2-Public-IP>:8080
Note: Ensure that port 8080 is allowed in the EC2 Security Group.

Useful Docker Commands
Command	Purpose
docker ps	List running containers
docker ps -a	List all containers
docker stop my-nginx	Stop a container
docker start my-nginx	Start a container
docker restart my-nginx	Restart a container
docker rm my-nginx	Remove a container
docker rmi nginx	Remove an image
Key Points
Docker Hub stores and distributes Docker images.

Images are used to create containers.

Port binding makes containerized applications accessible from the host machine.

Nginx can be deployed quickly using Docker.

Summary
Today's session focused on using Docker Hub and Docker images. I learned how to pull images, run containers, use port binding, and access containerized applications through a web browser. These concepts are essential for deploying applications with Docker.

This follows the same format as your previous diary entries:

✅ Short explanations (1–2 lines)

✅ Commands and syntax retained

✅ Practical steps emphasized

✅ Original wording

✅ Consistent style across your GitHub training diary


# Training Diary – Creating and Publishing Docker Images

**Date:** 01/07/2026

## Today I Learned

- Creating a custom Docker image
- Writing a Dockerfile in Visual Studio Code
- Building an image using Docker
- Running a container from the custom image
- Tagging a Docker image
- Pushing a Docker image to Docker Hub

---

## Training Notes

![Day 8 Notes](images/day8-notes.jpg)

---

# What is a Docker Image?

A Docker Image is a read-only template that contains everything required to run an application, including:

- Application source code
- Runtime environment
- Libraries
- Dependencies
- Configuration files

Images are used to create Docker containers.

---

# What is a Dockerfile?

A **Dockerfile** is a text file containing a set of instructions that Docker uses to automatically build an image.

Common Dockerfile Instructions:

| Instruction | Purpose |
|-------------|---------|
| FROM | Specifies the base image |
| WORKDIR | Sets the working directory |
| COPY | Copies files into the image |
| RUN | Executes commands while building the image |
| EXPOSE | Exposes a container port |
| CMD | Specifies the default command to run |

---

# Creating a Dockerfile in VS Code

### Step 1

Open **Visual Studio Code**.

### Step 2

Create a new project folder.

Example:

text
docker-demo


### Step 3

Inside the folder, create:

text
Dockerfile


Example Dockerfile:

dockerfile
FROM nginx:latest

COPY . /usr/share/nginx/html

EXPOSE 80


Save the file.

---

# Building a Docker Image

Open the terminal inside VS Code and navigate to the project folder.

Run:

bash
docker build -t my-nginx-image .


Explanation:

- docker build → Builds the image
- -t → Assigns a name (tag)
- . → Uses the current directory as the build context

---

# Checking Available Images

bash
docker images


This command lists all Docker images available on your system.

---

# Running the Custom Image

Run the image as a container:

bash
docker run -d -p 8080:80 --name mycontainer my-nginx-image


Explanation:

- -d → Detached mode
- -p → Port binding
- --name → Assign container name
- my-nginx-image → Image name

---

# Testing the Application

Open a browser and visit:

text
http://localhost:8080


If running on AWS EC2:

text
http://<Public-IP>:8080


The application should load successfully.

---

# Creating a Docker Hub Repository

### Step 1

Login to Docker Hub.

### Step 2

Click:

text
Create Repository


### Step 3

Enter:

- Repository Name
- Visibility (Public or Private)

Example:

text
username/my-nginx-image


---

# Login to Docker Hub from Terminal

bash
docker login


Enter your:

- Docker Hub Username
- Password

---

# Tagging an Image

Before pushing an image to Docker Hub, it must be tagged.

Syntax:

bash
docker tag <local-image> <dockerhub-username>/<repository-name>:latest


Example:

bash
docker tag my-nginx-image username/my-nginx-image:latest


---

# Pushing an Image to Docker Hub

Upload the image:

bash
docker push username/my-nginx-image:latest


Docker uploads the image layers to your Docker Hub repository.

---

# Verifying the Upload

Visit your Docker Hub account.

Open your repository.

The uploaded image will now be visible.

---

# Complete Docker Workflow

text
Create Project
      │
      ▼
Create Dockerfile
      │
      ▼
Build Docker Image
      │
      ▼
Run Docker Container
      │
      ▼
Test Application
      │
      ▼
Tag Docker Image
      │
      ▼
Login to Docker Hub
      │
      ▼
Push Image
      │
      ▼
Image Available on Docker Hub


---

# Important Docker Commands

Build Image

bash
docker build -t myimage .


List Images

bash
docker images


Run Container

bash
docker run -d -p 8080:80 myimage


Login to Docker Hub

bash
docker login


Tag Image

bash
docker tag myimage username/myimage:latest


Push Image

bash
docker push username/myimage:latest


Pull Image

bash
docker pull username/myimage:latest


---



# Summary

Dockerfiles automate the process of creating Docker images, ensuring consistent application environments. By building images locally and publishing them to Docker Hub, developers can easily share and deploy applications across different systems, making Docker an essential tool for modern software development and DevOps.

This one can also be made much cleaner and more like personal notes. Since you mentioned you also added a training notes image, I've kept a placeholder for it.

Day 8 Training Diary – Creating & Publishing Docker Images
Date: 01/07/2026

Topics Covered
Creating a Custom Docker Image

Dockerfile

Building Docker Images

Running Containers

Docker Image Tagging

Uploading Images to Docker Hub

Training Notes


Docker Image
A Docker Image is a template that contains everything required to run an application. It is used to create Docker containers.

Contents

Application code

Runtime

Libraries

Dependencies

Configuration files

Dockerfile
A Dockerfile is a text file that contains instructions for building a Docker image.

Common Instructions
Instruction	Purpose
FROM	Base image
WORKDIR	Set working directory
COPY	Copy project files
RUN	Execute commands
EXPOSE	Open a container port
CMD	Default command to run
Creating a Dockerfile
Steps performed:

Open Visual Studio Code.

Create a project folder.

docker-demo
Create a file named:

Dockerfile
Example:

FROM nginx:latest

COPY . /usr/share/nginx/html

EXPOSE 80
Building a Docker Image
Navigate to the project folder and run:

docker build -t my-nginx-image .
Explanation

docker build → Build the image

-t → Assign image name

. → Current directory

Check available images:

docker images
Running the Image
Create and run a container:

docker run -d -p 8080:80 --name mycontainer my-nginx-image
Open the application:

http://localhost:8080
For AWS EC2:

http://<EC2-Public-IP>:8080
Docker Hub Repository
Steps:

Sign in to Docker Hub.

Click Create Repository.

Enter the repository name.

Choose Public or Private.

Create the repository.

Example:

username/my-nginx-image
Docker Login
Login from the terminal:

docker login
Enter your Docker Hub username and password.

Tagging an Image
Before pushing an image, assign a Docker Hub tag.

docker tag my-nginx-image username/my-nginx-image:latest
Pushing an Image
Upload the image to Docker Hub.

docker push username/my-nginx-image:latest
Verify the uploaded image by opening your Docker Hub repository.

Docker Workflow
Project Files
      │
      ▼
Dockerfile
      │
      ▼
Build Image
      │
      ▼
Run Container
      │
      ▼
Test Application
      │
      ▼
Tag Image
      │
      ▼
Docker Login
      │
      ▼
Push to Docker Hub
Important Commands
Command	Purpose
docker build -t myimage .	Build an image
docker images	List images
docker run -d -p 8080:80 myimage	Run a container
docker login	Login to Docker Hub
docker tag myimage username/myimage:latest	Tag an image
docker push username/myimage:latest	Push image
docker pull username/myimage:latest	Pull image
Key Points
Dockerfile automates image creation.

Images can be built locally using docker build.

Docker Hub is used to store and share images.

Tagging is required before pushing an image.

Docker images can be downloaded and reused on any system.

Summary
Today's session focused on creating custom Docker images using a Dockerfile and publishing them to Docker Hub. I learned how to build, run, tag, and upload images, making it easier to share and deploy applications across different environments.

📌 Suggestion for the remaining diary
Your entries are now becoming very consistent. I recommend keeping this exact structure for every day:

Topics Covered (5–8 bullets)

Short theory (1–2 lines only)

Steps performed (if practical)

Commands/Tables (where needed)

Key Points (4–5 bullets)

Summary (2–3 lines)

This will make your GitHub repository look like a professional AWS & Docker Training Notes repository rather than copied class notes.


# Day 9 Training Diary – Docker Volumes & Dockerizing a React Project

**Date:** 02/07/2026

## Today I Learned

- Introduction to Docker Volumes
- Why Docker Volumes are used
- Creating and managing Docker Volumes
- Dockerizing a React application
- Creating a Dockerfile for a React project
- Using a .dockerignore file
- Building and running a Docker image for a React application

---



# Docker Volumes

Docker Volumes are used to store data outside the Docker container. They allow data to persist even if a container is stopped or deleted.

Volumes are managed by Docker and are the recommended way to store persistent application data.

## Advantages of Docker Volumes

- Persistent data storage
- Data is not lost when containers are removed
- Easy data sharing between containers
- Better performance than bind mounts
- Easy backup and restore

---

# Why Use Docker Volumes?

Without volumes:

- Container data is deleted when the container is removed.

With volumes:

- Data remains safely stored.
- New containers can reuse the same data.

Example:

text
Container
      │
      ▼
Docker Volume
      │
Persistent Data


---

# Basic Docker Volume Commands

## Create a Volume

bash
docker volume create myvolume


## List All Volumes

bash
docker volume ls


## Inspect a Volume

bash
docker volume inspect myvolume


## Remove a Volume

bash
docker volume rm myvolume


## Run a Container with a Volume

bash
docker run -d -v myvolume:/app/data nginx


Here,

- myvolume → Docker volume
- /app/data → Directory inside the container

---

# Dockerizing a React Project

Dockerizing means packaging a React application into a Docker image so that it can run consistently on any system.

---

# Steps to Dockerize a React Application

### Step 1

Create a React application.

bash
npx create-react-app my-react-app


### Step 2

Open the project in Visual Studio Code.

### Step 3

Create a file named:

text
Dockerfile


inside the project folder.

---

# Sample Dockerfile

dockerfile
FROM node:18

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE 3000

CMD ["npm","start"]


### Explanation

- FROM → Base image
- WORKDIR → Working directory
- COPY → Copies project files
- RUN → Installs dependencies
- EXPOSE → Opens port 3000
- CMD → Starts the React application

---

# Creating a .dockerignore File

A .dockerignore file tells Docker which files and folders should not be copied into the image.

Example:

text
node_modules
.git
.gitignore
README.md


## Benefits

- Smaller image size
- Faster build process
- Cleaner Docker image
- Improved security

---

# Build the Docker Image

Navigate to the project directory and run:

bash
docker build -t react-app .


---

# Verify the Image

bash
docker images


---

# Run the React Container

bash
docker run -d -p 3000:3000 --name react-container react-app


Explanation:

- -d → Run in background
- -p 3000:3000 → Port binding
- --name → Container name
- react-app → Image name

---

# Access the React Application

Open your browser and visit:

text
http://localhost:3000


If using an AWS EC2 instance:

text
http://<EC2-Public-IP>:3000


Ensure port **3000** is allowed in the EC2 Security Group.

---

# Useful Docker Commands

Build Image

bash
docker build -t react-app .


Run Container

bash
docker run -d -p 3000:3000 react-app


View Running Containers

bash
docker ps


Stop Container

bash
docker stop react-container


Start Container

bash
docker start react-container


Remove Container

bash
docker rm react-container


---


# Summary

Docker Volumes provide persistent storage for containerized applications, while Dockerizing a React project ensures that the application runs consistently across different environments. Using a Dockerfile and .dockerignore simplifies application deployment and follows best practices for containerized development.

Here's Day 9 in the same concise style as your previous entries. The theory is kept short, the wording is original, and it reads like personal training notes.

Day 9 Training Diary – Docker Volumes & Dockerizing a React Project
Date: 02/07/2026

Topics Covered
Docker Volumes

Managing Docker Volumes

Dockerizing a React Application

Creating a Dockerfile

Using a .dockerignore File

Building and Running a React Docker Image

Docker Volumes
Docker Volumes are used to store data outside a container so that it remains available even after the container is removed.

Advantages
Persistent data storage

Share data between containers

Easy backup and restore

Better performance than bind mounts

Why Use Docker Volumes?
Without a volume, container data is lost when the container is deleted. Volumes keep the data safe and allow it to be reused by new containers.

Container
     │
     ▼
Docker Volume
     │
Persistent Data
Docker Volume Commands
Create a volume:

docker volume create myvolume
List all volumes:

docker volume ls
Inspect a volume:

docker volume inspect myvolume
Remove a volume:

docker volume rm myvolume
Run a container with a volume:

docker run -d -v myvolume:/app/data nginx
Dockerizing a React Project
Dockerizing means packaging a React application into a Docker image so it can run consistently on different systems.

Steps Performed
Create a React application.

npx create-react-app my-react-app
Open the project in VS Code.

Create a Dockerfile.

Build the Docker image.

Run the container.

Sample Dockerfile
FROM node:18

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

EXPOSE 3000

CMD ["npm","start"]
.dockerignore File
The .dockerignore file excludes unnecessary files from the Docker image.

Example

node_modules
.git
.gitignore
README.md
Benefits
Smaller image size

Faster image build

Cleaner project structure

Build the Docker Image
docker build -t react-app .
Verify the image:

docker images
Run the React Container
docker run -d -p 3000:3000 --name react-container react-app
Open in the browser:

http://localhost:3000
For AWS EC2:

http://<EC2-Public-IP>:3000
Note: Ensure port 3000 is allowed in the EC2 Security Group.

Useful Docker Commands
Command	Purpose
docker build -t react-app .	Build Docker image
docker run -d -p 3000:3000 react-app	Run React container
docker ps	View running containers
docker stop react-container	Stop container
docker start react-container	Start container
docker rm react-container	Remove container
Key Points
Docker Volumes provide persistent storage.

Dockerfile automates image creation.

.dockerignore reduces image size.

React applications can be containerized for easy deployment.

Port mapping allows browser access to the application.

Summary
Today's session covered Docker Volumes and the process of Dockerizing a React application. I learned how to create volumes, write a Dockerfile, use a .dockerignore file, build a Docker image, and run the application inside a Docker container.

This keeps the same style we've established:

Short theory (1–2 lines)

Practical-first approach

Original wording

Important commands retained

Quick "Key Points" for revision

Your GitHub diary is now starting to look like a well-organized set of developer training notes rather than copied class material.




CMD ["npm","start"]
```

---

# .dockerignore File

The `.dockerignore` file excludes unnecessary files from the Docker image.

**Example**

```text
node_modules
.git
.gitignore
README.md
```

### Benefits

* Smaller image size
* Faster image build
* Cleaner project structure

---

# Build the Docker Image

```bash
docker build -t react-app .
```

Verify the image:

```bash
docker images
```

---

# Run the React Container

```bash
docker run -d -p 3000:3000 --name react-container react-app
```

Open in the browser:

```text
http://localhost:3000
```

For AWS EC2:

```text
http://<EC2-Public-IP>:3000
```

> **Note:** Ensure port **3000** is allowed in the EC2 Security Group.

---

# Useful Docker Commands

| Command                                | Purpose                 |
| -------------------------------------- | ----------------------- |
| `docker build -t react-app .`          | Build Docker image      |
| `docker run -d -p 3000:3000 react-app` | Run React container     |
| `docker ps`                            | View running containers |
| `docker stop react-container`          | Stop container          |
| `docker start react-container`         | Start container         |
| `docker rm react-container`            | Remove container        |

---

# Key Points

* Docker Volumes provide persistent storage.
* Dockerfile automates image creation.
* `.dockerignore` reduces image size.
* React applications can be containerized for easy deployment.
* Port mapping allows browser access to the application.

---

# Summary

Today's session covered Docker Volumes and the process of Dockerizing a React application. I learned how to create volumes, write a Dockerfile, use a `.dockerignore` file, build a Docker image, and run the application inside a Docker container.
